/* global PIXI */

export default class Actor extends PIXI.Container {
  constructor (game, object) {
    super()

    this._game = game
    this._object = object
  }

  update (delta) {

  }

  render (renderer) {

  }
}
